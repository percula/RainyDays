<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/nav_graph"
            app:startDestination="@id/locationFragment">

    <fragment
            android:id="@+id/locationFragment"
            android:name="dev.percula.rainydays.ui.LocationFragment"
            android:label="@string/app_name"
            tools:layout="@layout/fragment_location_list">
        <action
                android:id="@+id/action_locationFragment_to_locationPickerFragment"
                app:destination="@id/locationPickerFragment"/>
        <action
                android:id="@+id/action_locationFragment_to_settingsFragment"
                app:destination="@id/settingsFragment"/>
    </fragment>
    <dialog
            android:id="@+id/locationPickerFragment"
            android:name="dev.percula.rainydays.ui.LocationPickerFragment"
            tools:layout="@layout/fragment_location_picker"/>
    <fragment
            android:id="@+id/detailFragment"
            android:name="dev.percula.rainydays.ui.DetailFragment"
            tools:layout="@layout/fragment_detail">
        <argument
                android:name="location"
                app:argType="dev.percula.rainydays.model.Location"/>
    </fragment>
    <action
            android:id="@+id/action_global_detailFragment"
            app:destination="@id/detailFragment"/>
    <fragment
            android:id="@+id/settingsFragment"
            android:name="dev.percula.rainydays.ui.SettingsFragment"
            android:label="SettingsFragment"/>
</navigation>